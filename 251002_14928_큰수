	#include <iostream>

	using namespace std;

	int main() {
		ios_base::sync_with_stdio(false);
		cin.tie(NULL);

		string N;
		cin >> N;
		
		int ans = 0;
		for (int i = 0; i < N.size(); i++) {
			ans = (ans * 10 + (N[i] - '0')) % 20000303;
		}

		cout << ans;
	}
